schema = 1
name = "headless-live-claude"
description = "Exact Claude Code 2.1.229 core fixture for opt-in Trellage headless live verification."

[harness]
kind = "claude"
version = "2.1.229"

[harness.claude]
mode = "core"
default_auth = "proxy"
model = "claude-opus-5"
gateway = "http://copilot-proxy-rs:8080"

[image]
base = "node:22.17.0-bookworm-slim"
shell = "bash"
packages = ["bash", "ca-certificates", "gh", "git", "jq"]
