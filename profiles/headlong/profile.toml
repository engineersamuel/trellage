schema = 1
name = "headlong"
description = "Headlong persistent agent microharness with continuous background thinking, durable identity and trajectory, shell-based agency, and a loopback dashboard. After launch, type ada to enter the TUI."
skill_bundles = ["sandbox-common"]

[harness]
kind = "headlong"
version = "latest"

[image]
base = "node:22.17.0-bookworm-slim"
shell = "bash"
packages = ["bash", "ca-certificates", "curl", "gh", "git", "jq", "ripgrep"]
