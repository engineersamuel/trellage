schema = 1
name = "headlong"
description = "Headlong: a persistent, self-modifying agent world with identity, memory, background thinkers, and a loopback-only web dashboard. Use for long-running autonomous exploration; stop or destroy it explicitly when finished."
skill_bundles = ["sandbox-common"]

[harness]
kind = "headlong"
version = "latest"

[image]
base = "node:22.17.0-bookworm-slim"
shell = "bash"
packages = ["bash", "ca-certificates", "curl", "gh", "git", "jq", "ripgrep"]
