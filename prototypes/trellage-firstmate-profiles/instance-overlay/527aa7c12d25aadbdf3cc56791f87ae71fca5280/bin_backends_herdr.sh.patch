--- a/bin/backends/herdr.sh
+++ b/bin/backends/herdr.sh
@@ -351,6 +351,12 @@
 # when the PRIMARY spawns that secondmate (its own process's FM_HOME still
 # names the primary at that point) - see fm-spawn.sh's herdr case arm.
 fm_backend_herdr_workspace_label() {
+  if [ -n "${FMX_INSTANCE_ID:-}" ]; then
+    local controls
+    controls=$(python3 "${FMX_WORKER_LAUNCHER%/*}/fmx-controls.py" namespace) || return 1
+    printf '%s' "$controls" | jq -er .namespace
+    return
+  fi
   local marker="$FM_HOME/$FM_BACKEND_HERDR_SECONDMATE_MARKER" id
   if [ -f "$marker" ]; then
     id=$(tr -d '[:space:]' < "$marker" 2>/dev/null)
@@ -1966,6 +1972,10 @@
       *) return 3 ;;
     esac
   fi
+  if [ -n "${FMX_INSTANCE_ID:-}" ]; then
+    echo "error: named fleets require the captain's exact Herdr pane/workspace identity; label adoption is disabled" >&2
+    return 3
+  fi
   label=$(fm_backend_herdr_workspace_label)
   matches=$(fm_backend_herdr_workspace_find_all "$session")
   count=$(printf '%s' "$matches" | grep -c '[^[:space:]]' || true)
